# `data.*` — Strategy Spec Data-Node API Reference

> Generated by `docs/gen_data_api.py` from
> `cyqnt_trd/standard_bot/data/catalog.py`. **Do not hand-edit.**

The counterpart to `cyqnt_trd/blocks/BLOCKS_API.md`. `BLOCKS_API.md` documents the *compute* nodes (`indicators` / `conditions` / `entry` / …); this documents the *data* nodes a spec addresses as `type: data, function: <name>`.

```python
from cyqnt_trd.standard_bot.runtime import data, action

klines_1h = data.klines(symbol="BTCUSDT", interval="1h", limit=500)
funding   = data.funding(symbol="BTCUSDT", limit=500)
```

---

## Read this before choosing a node

**Availability is not a footnote — it decides whether your strategy can be backtested at all.** Most internal snapshot endpoints (Square social, futuresRadar, movement) are Redis-TTL reads with no point-in-time history. Replaying one reuses *today's* value at every historical bar, which produces a confident and completely fictional equity curve.

| tier | meaning | allowed in backtest |
|---|---|---|
| `BACKTESTABLE` | real history, deep enough to walk forward | yes |
| `SEMI` | history exists but is windowed / shallow / publication-lagged | only within the available window — check the first timestamp |
| `FORWARD_ONLY` | snapshot only — no replayable history; collect forward | no — live/paper only, strategy is RESEARCH_ONLY |
| `EXTERNAL_PENDING` | contract specified, feed not onboarded | no — feed not onboarded |
| `NOT_WIRED` | no endpoint exists anywhere | no |

`data.validate_nodes([...], for_backtest=True)` enforces this at compile time and quotes the specific hazard, so the failure lands before the run rather than as a flat curve after it.

**An empty result is not `ok`.** The Square APIs are Prod IP-whitelisted and answer off-whitelist calls with `success: true` and an empty body — indistinguishable from "nothing happened" unless the status is carried separately. Every call routes through the catalog, which grades an empty frame as `degraded`, never as zero.

---

## Index

| node | strategy types | availability | source |
|---|---|---|---|
| `data.account_balance` | * | `FORWARD_ONLY` | public_binance |
| `data.ahr999` | R | `BACKTESTABLE` | public_binance |
| `data.ai_signal` | C P | `FORWARD_ONLY` | internal_http |
| `data.ai_summary` | E R | `FORWARD_ONLY` | internal_http |
| `data.announcements` | E | `SEMI` | internal_http |
| `data.basis` | A D | `SEMI` | public_binance |
| `data.bdp_screen` | L P C S | `FORWARD_ONLY` | bdp_screening |
| `data.btc_dominance` | R | `EXTERNAL_PENDING` | external_vendor |
| `data.calendar` | E | `SEMI` | internal_http |
| `data.chip_distribution` | F | `FORWARD_ONLY` | internal_http |
| `data.cme_index` | R | `SEMI` | local_parquet |
| `data.coin_metrics` | L P N | `SEMI` | internal_http |
| `data.concentration` | F L | `SEMI` | internal_http |
| `data.contract_positions` | * | `FORWARD_ONLY` | internal_http |
| `data.cross_exchange` | A | `EXTERNAL_PENDING` | external_vendor |
| `data.equity_fundamentals` | R | `EXTERNAL_PENDING` | external_vendor |
| `data.etf_flow` | R E | `SEMI` | internal_http |
| `data.etf_metadata` | R | `EXTERNAL_PENDING` | external_vendor |
| `data.event_upcoming` | E | `SEMI` | internal_http |
| `data.fear_greed` | R E | `BACKTESTABLE` | public_binance |
| `data.funding` | D A N | `BACKTESTABLE` | public_binance |
| `data.funding_current` | D A | `FORWARD_ONLY` | public_binance |
| `data.funding_snapshot` | D L P | `FORWARD_ONLY` | public_binance |
| `data.funding_widget` | D A | `FORWARD_ONLY` | internal_http |
| `data.futures_radar` | D | `FORWARD_ONLY` | internal_http |
| `data.hot_coin` | R S | `FORWARD_ONLY` | internal_http |
| `data.hot_event` | E | `SEMI` | internal_http |
| `data.hot_post` | S E | `FORWARD_ONLY` | square_skill |
| `data.indicator_charts` | C P R | `BACKTESTABLE` | indicators_api |
| `data.klines` | C P L O A R | `BACKTESTABLE` | indicators_api |
| `data.klines_multi_tf` | C R | `BACKTESTABLE` | indicators_api |
| `data.large_flow` | F | `SEMI` | internal_http |
| `data.large_trade_info` | F O | `FORWARD_ONLY` | internal_http |
| `data.liquidations` | D F | `SEMI` | local_parquet |
| `data.long_short_ratio` | D | `SEMI` | public_binance |
| `data.macro_calendar` | R E | `SEMI` | internal_http |
| `data.macro_indicators` | R | `EXTERNAL_PENDING` | external_vendor |
| `data.market_scan` | L S | `FORWARD_ONLY` | public_binance |
| `data.news` | E | `FORWARD_ONLY` | square_skill |
| `data.news_search` | E S | `FORWARD_ONLY` | square_skill |
| `data.news_vendor` | E | `EXTERNAL_PENDING` | external_vendor |
| `data.onchain_signals` | F | `FORWARD_ONLY` | internal_http |
| `data.open_interest` | D F | `SEMI` | public_binance |
| `data.options_chain` | A | `EXTERNAL_PENDING` | external_vendor |
| `data.orderbook_depth` | O | `FORWARD_ONLY` | public_binance |
| `data.positions` | * | `FORWARD_ONLY` | public_binance |
| `data.premium_index` | A D | `SEMI` | public_binance |
| `data.sector_flow` | L N | `SEMI` | internal_http |
| `data.sentiment` | S | `FORWARD_ONLY` | square_skill |
| `data.social_x` | S | `EXTERNAL_PENDING` | external_vendor |
| `data.square_discussion` | S | `FORWARD_ONLY` | internal_http |
| `data.stablecoin_supply` | R F | `EXTERNAL_PENDING` | external_vendor |
| `data.strategy_ranking` | P | `FORWARD_ONLY` | internal_http |
| `data.taker_volume` | D O | `SEMI` | public_binance |
| `data.ticker_24h` | C S D | `FORWARD_ONLY` | public_binance |
| `data.ticker_rank` | S | `FORWARD_ONLY` | square_skill |
| `data.token_indicators` | C | `FORWARD_ONLY` | internal_http |
| `data.token_unlock` | E F | `SEMI` | internal_http |
| `data.top_player_movement` | F D | `FORWARD_ONLY` | internal_http |
| `data.top_trader_ratio` | D F | `SEMI` | public_binance |
| `data.topic_trending` | S | `FORWARD_ONLY` | square_skill |
| `data.universe` | L P S D | `FORWARD_ONLY` | public_binance |
| `data.user_favorites` | L S | `FORWARD_ONLY` | internal_http |
| `data.user_pnl` | * | `SEMI` | internal_http |
| `data.user_portfolio` | * | `FORWARD_ONLY` | internal_http |
| `data.web3_basket` | L S | `FORWARD_ONLY` | internal_http |
| `data.whale_trades` | F O | `SEMI` | local_parquet |

---

## Kline / technical

### `data.klines`

OHLCV candlesticks — the base series every technical block reads.

- **availability**: `BACKTESTABLE` — real history, deep enough to walk forward
- **source**: Technical Indicator API (base URL from `$INDICATORS_URL`)
- **endpoint**: indicators service (base URL from $INDICATORS_URL); public: data-api.binance.vision, api.binance.com/api/v3/klines, fapi/v1/klines
- **substitute**: cyqnt_trd.data_cli.fetch_klines (public Binance)
- **returns**: `pd.DataFrame` — one row per bar, oldest first · columns: `open_time`, `open`, `high`, `low`, `close`, `volume`, `quote_volume`, `close_time`, `trades`

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | yes |  | e.g. BTCUSDT |
| `interval` | `str` | no | `'1h'` | bar interval (`1m` / `5m` / `15m` / `30m` / `1h` / `4h` / `1d`) |
| `limit` | `int` | no | `500` | number of rows (most recent last) |
| `market_type` | `str` | no | `'futures'` | spot | futures (USDⓈ-M perpetual) (`spot` / `futures`) |

### `data.klines_multi_tf`

OHLCV for several intervals at once (multi-timeframe strategies).

- **availability**: `BACKTESTABLE` — real history, deep enough to walk forward
- **source**: Technical Indicator API (base URL from `$INDICATORS_URL`)
- **endpoint**: same as klines, intervals=[...]
- **returns**: `pd.DataFrame` — one frame, rows tagged by timeframe · columns: `open_time`, `open`, `high`, `low`, `close`, `volume`, `quote_volume`, `close_time`, `trades`
- **note**: A DAG can equally use N separate `klines` nodes; alignment happens inside the conditions blocks, not in the data layer.

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | yes |  | e.g. BTCUSDT |
| `intervals` | `list[str]` | yes |  | e.g. ["1h", "4h"] |
| `limit` | `int` | no | `500` | number of rows (most recent last) |
| `market_type` | `str` | no | `'futures'` | spot | futures (USDⓈ-M perpetual) (`spot` / `futures`) |

### `data.ticker_24h`

24h rolling price/volume statistics for one symbol.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: public Binance (`api`/`fapi`/`data-api.binance.vision`) or `alternative.me`
- **endpoint**: ticker24hr-price-change-statistics
- **returns**: `pd.DataFrame` — single row · columns: `symbol`, `price`, `change_pct`, `high_24h`, `low_24h`, `volume_base`, `volume_quote`, `trades`, `weighted_avg_price`
- **⚠️ PIT hazard**: rolling snapshot with no history endpoint — replay it and every bar sees today's 24h change. Derive from klines for backtests.

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | yes |  | e.g. BTCUSDT |
| `market_type` | `str` | no | `'futures'` | spot | futures (USDⓈ-M perpetual) (`spot` / `futures`) |

### `data.bdp_screen`

Cross-sectional screener fields (market_cap, taker_buy_pct, community_buzz, vol_mkt_cap, fdv_ratio, large_trade_pct, large_buy_pct + rsi/macd/kdj/ma/boll enum signals).

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: bdp screening (`source:"bdp"` condition clause)
- **endpoint**: screener /api/query with a condition clause carrying source:"bdp" (base URL from $BDP_SCREENING_URL)
- **returns**: `pd.DataFrame` — symbols passing the clause
- **⚠️ PIT hazard**: bdp exposes only the CURRENT snapshot. A walk-forward backtest reuses today's value at every bar — strictly lookahead. Honest for live selection at t=now; every bdp strategy must carry this note.
- **note**: Do not hand-roll a fetcher for these fields; express them as a clause.

| param | type | required | default | notes |
|---|---|---|---|---|
| `api_id` | `str` | yes |  | market_cap | taker_buy_pct | community_buzz | vol_mkt_cap | fdv_ratio | large_trade_pct | large_buy_pct | rsi_signal | macd_cross | kdj_cross | ma_cross | ema_cross | boll_cross |
| `type` | `str` | no | `'range'` | (`range` / `enum`) |
| `min` | `float` | no |  |  |
| `max` | `float` | no |  |  |
| `api_enum` | `str` | no |  | for type=enum |

### `data.indicator_charts`

Klines plus the 14 ta4j indicators in one call — the canonical in-domain source for C-type strategies.

- **availability**: `BACKTESTABLE` — real history, deep enough to walk forward
- **source**: Technical Indicator API (base URL from `$INDICATORS_URL`)
- **endpoint**: indicators service (base URL from $INDICATORS_URL)
- **returns**: `pd.DataFrame` — kline block joined with each requested indicator block · columns: `open_time`, `open`, `high`, `low`, `close`, `volume`, `quote_volume`, `close_time`, `trades`
- **note**: ta4j returns indicator VALUES; value->signal conversion (golden cross, RSI bands, BOLL breaks) is not provided upstream — use cyqnt_trd.blocks conditions for that.

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | yes |  | e.g. BTCUSDT |
| `interval` | `str` | no | `'1h'` | bar interval (`1m` / `5m` / `15m` / `30m` / `1h` / `4h` / `1d`) |
| `limit` | `int` | no | `500` | <= 2000 |
| `product` | `str` | no | `'um-perp'` | (`spot` / `um-perp` / `web3-alpha`) |
| `indicator_ids` | `list[str]` | no |  | ma/ema/rsi/macd/boll/kdj/atr/stdev/wr/sar/super/stochrsi/avl/obv; omit for klines only |
| `end_time` | `int` | no |  | UTC ms cut-off for replay |

### `data.token_indicators`

ta4j indicator values for one symbol (spot or futures).

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: /indicator/tokenIndicators{symbol,type,interval}
- **returns**: `pd.DataFrame` — latest indicator values
- **⚠️ PIT hazard**: serves the latest computed values; use indicator_charts with end_time for a replayable series.

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | yes |  | e.g. BTCUSDT |
| `product_type` | `str` | no | `'futures'` | (`spot` / `futures`) |
| `interval` | `str` | no | `'1h'` | bar interval (`1m` / `5m` / `15m` / `30m` / `1h` / `4h` / `1d`) |

### `data.ai_signal`

Existing hourly multi-factor composite signal + its factor list.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: /ai-skill/getAiSignal{symbol,type}
- **returns**: `pd.DataFrame` — single row · columns: `symbol`, `value`, `signal`, `long_win_rate`, `short_win_rate`, `avg_max_return`, `factors`
- **⚠️ PIT hazard**: snapshot. Coverage gap: source B is spot/alpha only — um-perp has no aiSignal coverage.
- **note**: This is also an output slot: our factor books are meant to extend currentFactors rather than duplicate it.

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | yes |  | e.g. BTCUSDT |
| `product_type` | `str` | no | `'spot'` | (`spot` / `alpha`) |

---

## Derivatives / crowding

### `data.funding`

Perpetual funding rate history — the cost of the crowded side.

- **availability**: `BACKTESTABLE` — real history, deep enough to walk forward
- **source**: public Binance (`api`/`fapi`/`data-api.binance.vision`) or `alternative.me`
- **endpoint**: fapi.binance.com/fapi/v1/fundingRate; internal syncFundingRate / futuresRadar/fundingRateWidget
- **returns**: `pd.DataFrame` — one row per 8h settlement, oldest first · columns: `symbol`, `rate`, `timestamp`, `mark_price`
- **note**: Years of history — this is what the funding-carry and crowding books were actually validated on.

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | yes |  | e.g. BTCUSDT |
| `limit` | `int` | no | `500` | settlements |

### `data.funding_snapshot`

Current funding-rate snapshot across every USDM perpetual; this is the cross-section a funding-based SELECTION strategy joins.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: public Binance (`api`/`fapi`/`data-api.binance.vision`) or `alternative.me`
- **endpoint**: fapi.binance.com/fapi/v1/premiumIndex (all symbols)
- **returns**: `pd.DataFrame` — one current row per perpetual symbol · columns: `symbol`, `lastFundingRate`, `markPrice`, `indexPrice`, `time`
- **⚠️ PIT hazard**: Current all-market snapshot only; capture it forward. It must not be replayed as historical funding at earlier decision times.
- **note**: Transport-only node. Live YAML selection stores it under the logical bundle key `funding`; the existing `funding` node remains the single-symbol settlement history used by trade strategies.

_(no parameters)_

### `data.funding_current`

Latest funding rate + next settlement time.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: public Binance (`api`/`fapi`/`data-api.binance.vision`) or `alternative.me`
- **endpoint**: fapi/v1/premiumIndex (mark-price)
- **returns**: `float` — current funding rate, e.g. 0.0001 = 0.01%
- **⚠️ PIT hazard**: point read; use `funding` for anything replayed.

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | yes |  | e.g. BTCUSDT |

### `data.open_interest`

Open-interest history (base + notional) for leverage build-up.

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: public Binance (`api`/`fapi`/`data-api.binance.vision`) or `alternative.me`
- **endpoint**: fapi futures/data/openInterestHist; internal syncLSAndOI
- **substitute**: local parquet open_interest_<period>.parquet via --derivatives-dir
- **returns**: `pd.DataFrame` — oldest first · columns: `timestamp`, `oi_base`, `oi_value`, `oi_change_bps`
- **⚠️ PIT hazard**: public endpoint keeps only ~30 days. A longer backtest silently starts mid-series — check the first timestamp, do not assume.

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | yes |  | e.g. BTCUSDT |
| `period` | `str` | no | `'1h'` | 5m..1d |
| `limit` | `int` | no | `48` |  |

### `data.long_short_ratio`

Global long/short ACCOUNT ratio (retail positioning skew).

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: public Binance (`api`/`fapi`/`data-api.binance.vision`) or `alternative.me`
- **endpoint**: fapi futures/data/globalLongShortAccountRatio
- **returns**: `pd.DataFrame` — oldest first · columns: `timestamp`, `long_account`, `short_account`, `long_short_ratio`
- **⚠️ PIT hazard**: ~30d only.
- **note**: NOT part of enrich_market_frame_with_derivatives — it does not appear as a df column on the trade route; read it as its own data node.

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | yes |  | e.g. BTCUSDT |
| `period` | `str` | no | `'1h'` |  |
| `limit` | `int` | no | `30` |  |

### `data.top_trader_ratio`

Top-trader long/short POSITION ratio (smart-money skew).

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: public Binance (`api`/`fapi`/`data-api.binance.vision`) or `alternative.me`
- **endpoint**: fapi futures/data/topLongShortPositionRatio
- **returns**: `pd.DataFrame` — oldest first · columns: `timestamp`, `long_account`, `short_account`, `long_short_ratio`
- **⚠️ PIT hazard**: ~30d only.

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | yes |  | e.g. BTCUSDT |
| `period` | `str` | no | `'1h'` |  |
| `limit` | `int` | no | `30` |  |

### `data.taker_volume`

Taker buy/sell volume ratio (aggressive order flow).

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: public Binance (`api`/`fapi`/`data-api.binance.vision`) or `alternative.me`
- **endpoint**: fapi futures/data/takerlongshortRatio
- **returns**: `pd.DataFrame` — oldest first · columns: `timestamp`, `buy_vol`, `sell_vol`, `buy_sell_ratio`
- **⚠️ PIT hazard**: ~30d only.
- **note**: Also available cross-sectionally as the bdp `taker_buy_pct` clause.

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | yes |  | e.g. BTCUSDT |
| `period` | `str` | no | `'1h'` |  |
| `limit` | `int` | no | `30` |  |

### `data.basis`

Perp premium/discount over the index (carry,期现偏离).

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: public Binance (`api`/`fapi`/`data-api.binance.vision`) or `alternative.me`
- **endpoint**: fapi futures/data/basis; premiumIndexKlines for the long form
- **returns**: `pd.DataFrame` — oldest first · columns: `timestamp`, `index_price`, `contract_price`, `basis`, `basis_rate`
- **⚠️ PIT hazard**: premiumIndex history is only ~400 days locally; ≥3yr backfill is an open item.

| param | type | required | default | notes |
|---|---|---|---|---|
| `pair` | `str` | yes |  | e.g. BTCUSDT |
| `period` | `str` | no | `'1h'` |  |
| `limit` | `int` | no | `30` |  |
| `contract_type` | `str` | no | `'PERPETUAL'` |  |

### `data.liquidations`

Aggregated forced-liquidation flow (cascade / capitulation).

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: local parquet under a `--*-dir` root
- **endpoint**: Binance WS !forceOrder@arr, recorded by HistoricalBinanceLiquidationRecorder into parquet
- **returns**: `pd.DataFrame` — bucketed by close_time · columns: `close_time`, `long_liquidation_usd`, `short_liquidation_usd`
- **⚠️ PIT hazard**: the WS stream is NOT archived upstream — you only have what this repo recorded. Coverage starts at the first capture, not at listing.

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | yes |  | e.g. BTCUSDT |
| `interval` | `str` | no | `'1h'` | bar interval (`1m` / `5m` / `15m` / `30m` / `1h` / `4h` / `1d`) |
| `liquidations_dir` | `str` | yes |  | root passed as --liquidations-dir |
| `frame` | `pd.DataFrame` | yes |  | bar frame to attach liquidation columns to (e.g. the klines node output) — must reference an upstream node output |
| `market_type` | `str` | no | `'futures'` | spot | futures (USDⓈ-M perpetual) (`spot` / `futures`) |

### `data.universe`

24h ticker table across the whole tradable universe — the input every SELECTION strategy ranks over.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: public Binance (`api`/`fapi`/`data-api.binance.vision`) or `alternative.me`
- **endpoint**: ticker24hr (all symbols)
- **returns**: `pd.DataFrame` — one row per symbol · columns: `symbol`, `price`, `change_pct`, `volume_quote`
- **⚠️ PIT hazard**: rolling 24h snapshot. For a replayed cross-section, rebuild the table from klines as of the decision bar.

| param | type | required | default | notes |
|---|---|---|---|---|
| `market_type` | `str` | no | `'futures'` | spot | futures (USDⓈ-M perpetual) (`spot` / `futures`) |

### `data.sector_flow`

Sector tags + sector net inflow (rotation, sector-neutral books).

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: rank/hotCategoryDetail, dim_asset_tag_relation, sector_net_inflow
- **returns**: `pd.DataFrame` — one row per sector · columns: `category`, `net_inflow`, `market_cap`, `change_pct`, `heat`, `members`
- **⚠️ PIT hazard**: 10-minute refresh snapshot; no bulk history endpoint.
- **note**: rank/hotCategoryDetail is the served path; dim_asset_tag_relation / sector_net_inflow are the warehouse tables behind it.

| param | type | required | default | notes |
|---|---|---|---|---|
| `sort` | `str` | no | `'netInflow'` |  |

### `data.options_chain`

Option chain (strike x expiry IV / greeks / OI) for the vol surface.

- **availability**: `EXTERNAL_PENDING` — contract specified, feed not onboarded
- **source**: third-party vendor feed (外采)
- **endpoint**: Deribit / Binance options (外采, onboarding)
- **returns**: `pd.DataFrame` — one row per contract
- **note**: Local t11_iv BVOL zips are the only implied-vol history on hand.

_(no parameters)_

### `data.futures_radar`

futuresRadar derivatives snapshot — funding / long-short / OI / buy-sell lists across the perp universe in one read.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: futuresRadar/getRadarDataForWidget, futuresRadar/getRadarData{indicatorKey}
- **returns**: `pd.DataFrame` — long form · columns: `symbol`, `metric`, `value`, `event_time`
- **⚠️ PIT hazard**: 5-minute snapshot, no history endpoint. Cross-sectional crowding read at t=now; use funding/open_interest for anything replayed.

| param | type | required | default | notes |
|---|---|---|---|---|
| `indicator_key` | `str` | no |  | omit for the widget bundle; set for one of the 70+ radar fields |

### `data.funding_widget`

Current funding rate + next settlement time (8h cadence).

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: futuresRadar/fundingRateWidget
- **returns**: `pd.DataFrame` — one row per symbol · columns: `symbol`, `funding_rate`, `next_funding_time`
- **⚠️ PIT hazard**: snapshot. next_funding_time is a forward prediction, not a settled value.

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | no |  | omit for the full list |

### `data.top_player_movement`

Top-trader position moves (smart-money follow / fade).

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: movement/getTopPlayerMovementList[ForSignal]
- **returns**: `pd.DataFrame` — TopTraderResponse rows
- **⚠️ PIT hazard**: snapshot only.

| param | type | required | default | notes |
|---|---|---|---|---|
| `for_signal` | `bool` | no | `False` |  |

### `data.coin_metrics`

Per-symbol market cap / liquidity / float (size + quality factors).

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: coinSelector/getMetricsCache (CoinMetrics-derived, 1h/4h)
- **returns**: `pd.DataFrame` — one row per symbol
- **⚠️ PIT hazard**: cache refreshed hourly; treat as slow-moving, not tick-accurate.

| param | type | required | default | notes |
|---|---|---|---|---|
| `token` | `str` | no |  | omit for the full universe |

### `data.premium_index`

Perp mark-vs-index premium series (the long form of basis).

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: public Binance (`api`/`fapi`/`data-api.binance.vision`) or `alternative.me`
- **endpoint**: fapi/v1/premiumIndexKlines; vision futures/um premiumIndexKlines archive
- **returns**: `pd.DataFrame` — OHLC of the premium index · columns: `open_time`, `open`, `high`, `low`, `close`, `volume`, `quote_volume`, `close_time`, `trades`
- **⚠️ PIT hazard**: archive currently holds ~400 days; a >=3yr backfill is an open item.

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | yes |  | e.g. BTCUSDT |
| `interval` | `str` | no | `'1h'` | bar interval (`1m` / `5m` / `15m` / `30m` / `1h` / `4h` / `1d`) |
| `limit` | `int` | no | `500` | number of rows (most recent last) |

### `data.cross_exchange`

OKX / Bybit klines, depth and trades for cross-venue spreads.

- **availability**: `EXTERNAL_PENDING` — contract specified, feed not onboarded
- **source**: third-party vendor feed (外采)
- **endpoint**: self-collected cross-exchange feed (item 65)
- **returns**: `pd.DataFrame` — per-venue quotes · columns: `open_time`, `open`, `high`, `low`, `close`, `volume`, `quote_volume`, `close_time`, `trades`

_(no parameters)_

---

## Orderbook / microstructure

### `data.orderbook_depth`

L2 depth snapshot and derived imbalance.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: public Binance (`api`/`fapi`/`data-api.binance.vision`) or `alternative.me`
- **endpoint**: fapi/v1/depth (live snapshot only)
- **returns**: `pd.DataFrame` — one row per price level, best-first within each side · columns: `side`, `price`, `qty`
- **⚠️ PIT hazard**: NO archive exists for top-20 L2 anywhere. The only replayable proxy is the 30s bookDepth band parquet (BTC/ETH/SOL). Any O-type backtest is on a proxy, and no live WS pipeline exists yet.

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | yes |  | e.g. BTCUSDT |
| `limit` | `int` | no | `100` |  |
| `market_type` | `str` | no | `'futures'` | spot | futures (USDⓈ-M perpetual) (`spot` / `futures`) |

### `data.whale_trades`

Whale-sized trade aggregation (5-min buy/sell bars).

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: local parquet under a `--*-dir` root
- **endpoint**: local autotrade/t15_whale/data bars_*_5min
- **returns**: `pd.DataFrame` — 5-min bars · columns: `time`, `tot_buy`, `tot_sell`, `whale_buy`, `whale_sell`, `whale_cnt`, `n_trades`
- **⚠️ PIT hazard**: local capture is BTC-only and ~40 days (2026-04-25..06-03).

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | yes |  | e.g. BTCUSDT |

### `data.large_trade_info`

Large buy/sell prints for one token (block-trade pressure).

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: hot/getHotCoinDetailForLargeTradeInfo{token}
- **returns**: `pd.DataFrame` — prints · columns: `time`, `side`, `volume`, `volume_usdt`
- **⚠️ PIT hazard**: snapshot window only.

| param | type | required | default | notes |
|---|---|---|---|---|
| `token` | `str` | yes |  | base token, e.g. BTC (no quote) |

---

## News / event

### `data.news`

Binance Square news + announcements (listings, delistings, maintenance).

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: Binance Square skill API (Prod IP-whitelisted)
- **endpoint**: square/skill/getNews{lang,pageIndex,pageSize}
- **returns**: `pd.DataFrame` — one row per post · columns: `id`, `title`, `summary`, `body`, `web_link`, `date`, `content_type`, `tendency`, `author_name`, `author_role`, `tickers`, `generated_at`
- **⚠️ PIT hazard**: Prod IP-whitelisted; off-whitelist returns success:true with an EMPTY data block, which is indistinguishable from 'no news' unless you check source_status. No bulk history — collect forward.

| param | type | required | default | notes |
|---|---|---|---|---|
| `lang` | `str` | no | `'en'` |  |
| `page_size` | `int` | no | `50` |  |

### `data.news_search`

Square keyword/author search — community leads, lower reliability.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: Binance Square skill API (Prod IP-whitelisted)
- **endpoint**: square/skill/getSearch{keyword,author,minLikes,window}
- **returns**: `pd.DataFrame` — same shape as news
- **⚠️ PIT hazard**: community content, not an official source. Treat as a lead: no official link and no second independent source -> investigate only.

| param | type | required | default | notes |
|---|---|---|---|---|
| `keyword` | `str` | no |  | at least one of keyword/author/min_likes |
| `author` | `str` | no |  |  |
| `min_likes` | `int` | no | `0` |  |
| `window` | `str` | no | `'24h'` | aggregation window (`1h` / `4h` / `24h` / `3d` / `7d`) |

### `data.hot_post`

Square hot posts by heat / time / engagement.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: Binance Square skill API (Prod IP-whitelisted)
- **endpoint**: square/skill/getHotPost{sort,window,limit}
- **returns**: `pd.DataFrame` — post list + generated_at
- **⚠️ PIT hazard**: 60s cache snapshot, no history.

| param | type | required | default | notes |
|---|---|---|---|---|
| `sort` | `str` | no | `'HEAT'` | (`HEAT` / `TIME` / `ENGAGEMENT`) |
| `window` | `str` | no | `'24h'` | aggregation window (`1h` / `4h` / `24h` / `3d` / `7d`) |
| `limit` | `int` | no | `20` |  |

### `data.fear_greed`

Crypto Fear & Greed index (0-100) — top-level risk regime.

- **availability**: `BACKTESTABLE` — real history, deep enough to walk forward
- **source**: public Binance (`api`/`fapi`/`data-api.binance.vision`) or `alternative.me`
- **endpoint**: internal market/fearAndGreedHistory; public alternative.me/fng/?limit=0
- **returns**: `pd.DataFrame` — daily, oldest first · columns: `date`, `value`, `value_classification`
- **note**: Deep daily history from 2023-06-28 — genuinely backtestable.

| param | type | required | default | notes |
|---|---|---|---|---|
| `limit` | `int` | no | `0` | 0 = full history |

### `data.etf_flow`

Spot-ETF daily net flow and AUM by issuer.

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: market/ETFHistory{token}, market/ETFLatest
- **returns**: `pd.DataFrame` — daily · columns: `token`, `date`, `flow`, `net_assets`, `close_price`
- **⚠️ PIT hazard**: T+1 publication lag — using the same-day value is lookahead. close_price is frequently 0/null.
- **note**: Per-issuer detail via market/ETFTickerDetail; ETFLatest for the current snapshot.

| param | type | required | default | notes |
|---|---|---|---|---|
| `token` | `str` | yes |  | base token, e.g. BTC (no quote) |

### `data.hot_event`

Curated hot events with rank, sentiment and related coins.

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: event/getHotEventForSignal{category}, event/getHotEventDetail
- **returns**: `pd.DataFrame` — one row per event · columns: `id`, `publish_time_ts`, `news_rank`, `sentiment`, `category`, `summary`, `related_coins`, `backtest_list`
- **⚠️ PIT hazard**: news_rank and sentiment can be revised AFTER publication — only publish_time_ts is trustworthy at signal time.

| param | type | required | default | notes |
|---|---|---|---|---|
| `category` | `str` | no |  |  |

### `data.event_upcoming`

Forward-looking event cards (listings, upgrades, unlocks).

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: ti/event/getEventUpcoming
- **returns**: `pd.DataFrame` — one row per card · columns: `card_coin`, `signal_type`, `signal_time`, `signal_expired_time`, `is_bullish`, `price_change_1`, `price_change_2`, `price_change_3`
- **⚠️ PIT hazard**: price_change_1/2/3 are BACKFILLED after the event resolves. Using them as features is direct label leakage — never read them at signal time.

_(no parameters)_

### `data.macro_calendar`

Scheduled macro releases (CPI / NFP / FOMC / rate decisions).

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: calendar/macroDetail
- **returns**: `pd.DataFrame` — one row per release · columns: `event_time`, `event_type`, `actual`, `forecast`, `previous`
- **⚠️ PIT hazard**: PIT-safe on the schedule (release times are known in advance), but `actual` only exists after the release — gate on event_time.

_(no parameters)_

### `data.token_unlock`

Token unlock schedule and history.

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: calendar/unlockDetail, calendar/dailyCalendarForWidget
- **returns**: `pd.DataFrame` — one row per unlock · columns: `token`, `unlock_time`, `total_unlocked`, `next_unlock`, `price`
- **⚠️ PIT hazard**: forward schedule only, no bulk history — snapshot forward.
- **note**: Feeds the catalyst_risk_monitor unlock rule.

_(no parameters)_

### `data.calendar`

Listing / unlock / activity calendar (forward schedule).

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: calendar/dailyCalendarForWidget, calendar/monthlyCalendarForWidget
- **returns**: `pd.DataFrame` — one row per scheduled event · columns: `event_id`, `event_time`, `event_type`, `coin`, `event_title`, `deeplink`
- **⚠️ PIT hazard**: forward schedule; no bulk history. event_time is known ahead, so gating on it is PIT-safe.

| param | type | required | default | notes |
|---|---|---|---|---|
| `period` | `str` | no | `'daily'` | (`daily` / `monthly`) |
| `date` | `str` | no |  | YYYY-MM-DD / YYYY-MM |

### `data.announcements`

Binance official announcements with token linkage and impact tag.

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: binance-announcement service (D09)
- **returns**: `pd.DataFrame` — one row per announcement · columns: `announce_time`, `title`, `tokens`, `sentiment`, `category`, `impact`
- **⚠️ PIT hazard**: publication time is trustworthy; the derived sentiment/impact tags are model output and may be re-scored later.
- **note**: Distinct from Square getNews: this is the official announcement feed.

_(no parameters)_

### `data.news_vendor`

Third-party news wire (CoinDesk / CoinTelegraph / Reuters).

- **availability**: `EXTERNAL_PENDING` — contract specified, feed not onboarded
- **source**: third-party vendor feed (外采)
- **endpoint**: news vendor API (item 84); raw newsText RAG stream is item 85
- **returns**: `pd.DataFrame` — one row per article
- **note**: The timestamped per-token raw stream (85) is what an LLM event detector needs; the wire feed alone is headlines.

_(no parameters)_

### `data.ai_summary`

LLM-generated market summary / report body for the home feed.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: aiSummary/getAiSummaryForHome (5-10min refresh)
- **returns**: `pd.DataFrame` — summary blocks
- **⚠️ PIT hazard**: regenerated every few minutes with no archive; it is model output over other feeds, not an independent observation.
- **note**: Use as user-facing copy, not as a strategy feature — it restates inputs the bot can read directly.

_(no parameters)_

---

## Social / attention

### `data.ticker_rank`

Square per-ticker mention/engagement/sentiment aggregates.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: Binance Square skill API (Prod IP-whitelisted)
- **endpoint**: square/skill/getTickerRank{window,limit,lang} (Prod only)
- **returns**: `pd.DataFrame` — one row per ticker · columns: `ticker`, `mention_count`, `unique_authors`, `total_engagement`, `bullish_count`, `bearish_count`, `neutral_count`, `rank`, `generated_at`
- **⚠️ PIT hazard**: `ticker` is CASE-SENSITIVE — do not upper() it before matching. 60s cache, no history: forward-collect or the backtest is fiction.

| param | type | required | default | notes |
|---|---|---|---|---|
| `window` | `str` | no | `'24h'` | aggregation window (`1h` / `4h` / `24h` / `3d` / `7d`) |
| `limit` | `int` | no | `20` |  |
| `lang` | `str` | no | `'en'` |  |

### `data.sentiment`

Square bull/bear poll for one token.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: Binance Square skill API (Prod IP-whitelisted)
- **endpoint**: square/skill/getSentiment{token}
- **returns**: `pd.DataFrame` — single row · columns: `bullish_value`, `bearish_value`, `total_value`, `bull_ratio`
- **⚠️ PIT hazard**: a single live poll, latest snapshot only.

| param | type | required | default | notes |
|---|---|---|---|---|
| `token` | `str` | yes |  | base token, e.g. BTC (no quote) |

### `data.topic_trending`

Square trending hashtags/topics with windowed engagement.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: Binance Square skill API (Prod IP-whitelisted)
- **endpoint**: square/skill/getTopicTrending{window,limit,lang}
- **returns**: `pd.DataFrame` — one row per topic · columns: `id`, `hashtag`, `window_mention_count`, `window_unique_authors`, `window_total_engagement`, `window_bullish_count`
- **⚠️ PIT hazard**: snapshot, no history.

| param | type | required | default | notes |
|---|---|---|---|---|
| `window` | `str` | no | `'24h'` | aggregation window (`1h` / `4h` / `24h` / `3d` / `7d`) |
| `limit` | `int` | no | `20` |  |

### `data.market_scan`

Full-market scan with filters (gainers/losers/volume screens).

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: public Binance (`api`/`fapi`/`data-api.binance.vision`) or `alternative.me`
- **endpoint**: ticker24hr all-symbol + client-side filter
- **returns**: `pd.DataFrame` — filtered symbol table
- **⚠️ PIT hazard**: snapshot.

| param | type | required | default | notes |
|---|---|---|---|---|
| `market_type` | `str` | no | `'futures'` | spot | futures (USDⓈ-M perpetual) (`spot` / `futures`) |

### `data.hot_coin`

BTC / top-10 quote snapshot (10s upstream refresh).

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: hot/getHotCoin
- **returns**: `pd.DataFrame` — one row per hot coin
- **⚠️ PIT hazard**: snapshot.

_(no parameters)_

### `data.user_favorites`

User watchlist / favourites — the universe a personal bot ranks.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: favorite/getUserFavorite
- **returns**: `pd.DataFrame` — one row per symbol
- **⚠️ PIT hazard**: live state.

| param | type | required | default | notes |
|---|---|---|---|---|
| `user_id` | `str` | no |  |  |

### `data.square_discussion`

Normalised Square discussion heat and KOL summary per token.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: hot_discussion_content_summary
- **returns**: `pd.DataFrame` — one row per token · columns: `symbol`, `bull_bear_ratio`, `heat_0_100`, `kol_summary`
- **⚠️ PIT hazard**: derived snapshot over the Square feed; inherits its no-history limit.

_(no parameters)_

### `data.social_x`

X / Twitter volume, bull-bear split and KOL activity.

- **availability**: `EXTERNAL_PENDING` — contract specified, feed not onboarded
- **source**: third-party vendor feed (外采)
- **endpoint**: X API (item 90)
- **returns**: `pd.DataFrame` — one row per token per window

_(no parameters)_

### `data.web3_basket`

Web3 / alpha token top-10 basket quotes.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: syncWeb3PriceJob / syncTrendingHistory
- **returns**: `pd.DataFrame` — one row per basket member
- **⚠️ PIT hazard**: minute snapshot; membership itself rotates without an archive, so a replay silently uses today's basket.

_(no parameters)_

---

## Cross-section / universe

### `data.strategy_ranking`

Ranked strategies per symbol with return / sharpe / win-rate / maxDD.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: /ai-skill/getStrategyRanking{symbol,type}
- **returns**: `pd.DataFrame` — top-N rows · columns: `rank`, `total_return`, `sharpe`, `win_rate`, `max_dd`, `avg_hold`, `symbol`
- **⚠️ PIT hazard**: snapshot of an already-fitted ranking; using it as a feature is borrowing someone else's hindsight.
- **note**: Also the production slot our own strategy_ranking output plugs into.

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | yes |  | e.g. BTCUSDT |
| `product_type` | `str` | no | `'spot'` |  |

### `data.concentration`

On-venue holding concentration score per symbol (daily).

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: warehouse table bnb_dwa.spottrading_page_concentration_score_day
- **returns**: `pd.DataFrame` — daily · columns: `date`, `symbol`, `concentration_score`
- **⚠️ PIT hazard**: daily batch; same-day value is not available intraday.

_(no parameters)_

---

## Flow / whale

### `data.large_flow`

Large deposit/withdraw (exchange in/out) signal per token.

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: movement/getLargeDepositWithdrawData{token,interval}
- **returns**: `pd.DataFrame` — 1h windows · columns: `window_start_time`, `total_deposit_qty`, `total_withdraw_qty`, `large_deposit_amt`, `large_withdraw_amt`, `total_signal`
- **⚠️ PIT hazard**: 1h windowed history exists but is shallow; internal-only.

| param | type | required | default | notes |
|---|---|---|---|---|
| `token` | `str` | yes |  | base token, e.g. BTC (no quote) |
| `interval` | `str` | no | `'1h'` |  |

### `data.chip_distribution`

Cost-basis (chip) distribution — where holders are under water.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: movement/getChipDistributionData{token}
- **returns**: `pd.DataFrame` — price buckets with held volume
- **⚠️ PIT hazard**: snapshot only.

| param | type | required | default | notes |
|---|---|---|---|---|
| `token` | `str` | yes |  | base token, e.g. BTC (no quote) |

### `data.onchain_signals`

On-chain anomaly signals (FOMO / PANIC / LARGE_INVESTORS / LIQUIDITY_SURGE) with direction and strength.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: onchain.signal service
- **returns**: `pd.DataFrame` — one row per signal · columns: `event_time`, `symbol`, `signal_type`, `direction`, `strength`
- **⚠️ PIT hazard**: event stream is emitted forward; no replayable archive.

_(no parameters)_

### `data.stablecoin_supply`

Stablecoin mint / burn — aggregate dry powder entering the market.

- **availability**: `EXTERNAL_PENDING` — contract specified, feed not onboarded
- **source**: third-party vendor feed (外采)
- **endpoint**: on-chain supply feed (item 86)
- **substitute**: local autotrade/t21_stablecoin 1m parquet (USDC, FDUSD) for research
- **returns**: `pd.DataFrame` — daily supply delta per issuer

_(no parameters)_

---

## Regime / macro / cross-asset

### `data.ahr999`

AHR999 BTC valuation index — accumulation-zone regime.

- **availability**: `BACKTESTABLE` — real history, deep enough to walk forward
- **source**: public Binance (`api`/`fapi`/`data-api.binance.vision`) or `alternative.me`
- **endpoint**: internal ahr999/getAhr999List; public: reconstruct from BTC close
- **returns**: `pd.DataFrame` — daily · columns: `timestamp`, `date`, `close`, `ahr999`, `geomean_200d`, `model_price`

| param | type | required | default | notes |
|---|---|---|---|---|
| `limit` | `int` | no | `1000` | number of daily points (most recent last) |

### `data.cme_index`

CME index futures (NQ / ES) bars for cross-asset regime.

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: local parquet under a `--*-dir` root
- **endpoint**: local parquet ingested by standard_bot.data.cme (yfinance / HuggingFace / CSV)
- **returns**: `pd.DataFrame` — OHLCV bars · columns: `open_time`, `open`, `high`, `low`, `close`, `volume`, `quote_volume`, `close_time`, `trades`
- **⚠️ PIT hazard**: ingested history only; check the ingested span before backtesting.

| param | type | required | default | notes |
|---|---|---|---|---|
| `instrument_id` | `str` | yes |  | e.g. NQ=F |
| `interval` | `str` | no | `'1h'` | bar interval (`1m` / `5m` / `15m` / `30m` / `1h` / `4h` / `1d`) |

### `data.btc_dominance`

BTC dominance and total crypto market cap — top-level risk switch.

- **availability**: `EXTERNAL_PENDING` — contract specified, feed not onboarded
- **source**: third-party vendor feed (外采)
- **endpoint**: CMC vendor feed (b9-design page 590233601)
- **returns**: `pd.DataFrame` — daily · columns: `date`, `btc_dominance`, `total_market_cap`
- **note**: Rated the single most valuable genuinely-new external field for crypto. Not onboarded yet — declare it, do not fake it.

_(no parameters)_

### `data.macro_indicators`

DXY / UST yields / VIX / PMI — cross-asset risk backdrop.

- **availability**: `EXTERNAL_PENDING` — contract specified, feed not onboarded
- **source**: third-party vendor feed (外采)
- **endpoint**: FX / UST / CBOE vendor feeds (外采, item 88)
- **returns**: `pd.DataFrame` — daily series per indicator

_(no parameters)_

### `data.equity_fundamentals`

US equity fundamentals (PE/PB/ROE/EPS/margin) for stock tokens.

- **availability**: `EXTERNAL_PENDING` — contract specified, feed not onboarded
- **source**: third-party vendor feed (外采)
- **endpoint**: equity fundamentals vendor (items 70-72)
- **returns**: `pd.DataFrame` — one row per ticker per period

_(no parameters)_

### `data.etf_metadata`

ETF category / index / expense / issuer / leverage / AUM tiers.

- **availability**: `EXTERNAL_PENDING` — contract specified, feed not onboarded
- **source**: third-party vendor feed (外采)
- **endpoint**: ETF.com / Morningstar / issuer -> etf_metadata (items 75-77, 83)
- **returns**: `pd.DataFrame` — one row per ETF

_(no parameters)_

---

## Account

### `data.account_balance`

Account balances (sizing / risk inputs).

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: public Binance (`api`/`fapi`/`data-api.binance.vision`) or `alternative.me`
- **endpoint**: binance-cli account endpoints (authenticated)
- **returns**: `pd.DataFrame` — asset -> free/locked
- **⚠️ PIT hazard**: live account state; never available historically. A backtest must use the simulated equity curve, not this node.

_(no parameters)_

### `data.positions`

Open positions (exposure / reduce-only checks).

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: public Binance (`api`/`fapi`/`data-api.binance.vision`) or `alternative.me`
- **endpoint**: binance-cli futures position endpoints (authenticated)
- **returns**: `pd.DataFrame` — one row per position
- **⚠️ PIT hazard**: live only.

_(no parameters)_

### `data.user_portfolio`

User holdings with cost basis, PnL and idle days.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: binance-portfolio getPortfolio
- **returns**: `pd.DataFrame` — one row per holding · columns: `symbol`, `quantity`, `entry_price`, `unrealized_pnl`
- **⚠️ PIT hazard**: live account state; a backtest must use the simulated equity curve.

| param | type | required | default | notes |
|---|---|---|---|---|
| `user_id` | `str` | no |  |  |

### `data.contract_positions`

Open perpetual positions: direction, size, leverage, margin ratio, unrealised PnL. This is what the position lifecycle reads to know whether an exit means CLOSE_LONG or CLOSE_SHORT.

- **availability**: `FORWARD_ONLY` — snapshot only — no replayable history; collect forward
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: 合约持仓 BAPI; /ws/tokenList (sub-second SLA)
- **returns**: `pd.DataFrame` — one row per open position · columns: `symbol`, `side`, `quantity`, `entry_price`, `leverage`, `margin_ratio`, `unrealized_pnl`
- **⚠️ PIT hazard**: live account state. A backtest must use the simulated book, never this node.

| param | type | required | default | notes |
|---|---|---|---|---|
| `symbol` | `str` | no |  | restrict to one instrument; omit for the whole book |
| `profile` | `str` | no | `'default'` | local credential profile to read the account of |

### `data.user_pnl`

Per-period, per-coin realised/unrealised PnL and risk slices.

- **availability**: `SEMI` — history exists but is windowed / shallow / publication-lagged
- **source**: internal-network HTTP endpoint (host and route come from the private deployment config, not from this repo)
- **endpoint**: 用户 PnL SyncRateJob / TokenPriceJob
- **returns**: `pd.DataFrame` — period x symbol PnL
- **⚠️ PIT hazard**: batch-computed per period; intraday values lag.

| param | type | required | default | notes |
|---|---|---|---|---|
| `user_id` | `str` | no |  |  |
| `period` | `str` | no | `'30d'` |  |

---

## Codegen contract

- Call form is `data.<function>(**params)` — the same one-rule derivation the
  compute nodes use, so no mapping table is needed.
- `import` line: `from cyqnt_trd.standard_bot.runtime import data, action`.
- Param keys above are exactly the spec `params[].key` values.
- Wrap a whole decision cycle in `with data.session() as s:` so every node in
  the DAG shares one `as_of`; `s.status_map()` then feeds
  `SnapshotMeta.source_status` and `s.warnings()` feeds `SnapshotMeta.warnings`.
- A failed fetch raises `data.DataUnavailable`; `data.collect([...])` degrades
  instead, returning the frames it got plus the per-node status.

_67 nodes: 6 BACKTESTABLE, 21 SEMI, 31 FORWARD_ONLY, 9 EXTERNAL_PENDING._
